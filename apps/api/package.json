{
    "name": "api",
    "scripts": {
        "dev": "bash -c 'set -a; [ -f .env ] && source .env; set +a; source .venv/bin/activate && uvicorn app.main:app --reload --host ${API_HOST:-0.0.0.0} --port ${API_PORT:-8000}'"
    }
}
